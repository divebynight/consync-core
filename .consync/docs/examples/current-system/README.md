# Current System Example

This directory is the canonical example of the current expected Consync process and state layout.

The live files in `.consync/state/` are the operational truth.

This example shows the intended structure and the expected shape of the active state files without pretending to be live project state.

Git history preserves prior versions of this example, so the example itself should stay small and easy to update.